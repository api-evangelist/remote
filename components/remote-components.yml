generated: '2026-07-20'
method: searched
source: https://developer.remote.com/docs/remote-flows-sdk
summary: >-
  Remote ships the Remote Flows SDK — a React component + hooks library that
  embeds Remote's employment workflows directly into partner UIs. Two
  integration paths: drop-in pre-built components, or headless hooks that let
  partners render their own UI over Remote's business logic.
families:
  - name: Remote Flows (React)
    loader: "@remoteoss/remote-flows"
    repository: https://github.com/remoteoss/remote-flows
    modes:
      - drop-in components (plug-and-play React)
      - headless hooks (custom UI, Remote business logic)
    flows:
      - Cost estimation / cost calculator
      - Onboarding
      - Offboarding
      - Contract amendments
  - name: JSON Schema Form
    loader: "@remoteoss/json-schema-form"
    repository: https://github.com/remoteoss/json-schema-form
    description: >-
      Headless, JSON-Schema-driven form renderer that powers the country-specific
      dynamic forms inside the Flows SDK.
